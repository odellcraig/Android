<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fivethreeapps.chore"
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-sdk android:minSdkVersion="8" />

    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.CAMERA" />
 	<uses-feature android:name="android.hardware.camera" />
 	<uses-feature android:name="android.hardware.camera.autofocus" />
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".activity.PreviewActivity" android:label="@string/tabLabelPreview" />
		<activity android:name=".activity.SetupActivity" android:label="@string/tabLabelSetup" />
		<activity android:name=".activity.ChildActivityEdit" android:label="@string/labelChildren" />
		<activity android:name=".activity.ChoreListActivity" android:label="@string/labelChores" />
		<activity android:name=".activity.ChoreEditActivity" android:label="@string/labelChores" />
		<activity android:name=".activity.RewardListActivity" android:label="@string/labelRewards" />
		<activity android:name=".activity.RewardEditActivity" android:label="@string/labelRewards" />
    </application>

</manifest>